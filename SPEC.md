# lync Spec

The public format specification now lives in [FORMAT.md](./FORMAT.md).

This file remains as a compatibility pointer for older links. Treat
`FORMAT.md` as the source of truth for `.lync` files, event envelopes, byte
handling, union, diagnostics, and view eligibility.
